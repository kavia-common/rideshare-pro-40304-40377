# PUBLIC_INTERFACE
# Example environment configuration for the backend server.

# Port the Express server listens on
PORT=4000

# Must exactly match the frontend origin (protocol+host+port)
FRONTEND_URL=http://localhost:3000

# JWT signing secret (use a strong value in production)
JWT_SECRET=dev-secret

# WebSocket path (optional; defaults to /ws)
WS_PATH=/ws

# Log level: debug|info|warn|error
LOG_LEVEL=info
